import { LoginForm } from "./LoginForm"
import { Box } from "@mui/material"

export const Login = () => {
  return (
      <Box component="div" sx={{ marginTop: 10 }}> 
          <LoginForm />
      </Box>
    )
}
